SELECT SAL.SAL_DT, 
				SUM(SAL.SAL_CNT*PDT.PRICE) TOTAL_SAL
		
		FROM PRODUCT_TB PDT,
		  		SALE_TB SAL,
				STORE_TB STR
				
		WHERE STR.STO_ID = #{stoId} AND
				SAL.SAL_DT BETWEEN #{startDt} AND #{endDt} AND
				PDT.PDT_NO = SAL.PDT_NO AND
				STR.STO_ID = SAL.STO_ID
		
		GROUP BY SAL.SAL_DT